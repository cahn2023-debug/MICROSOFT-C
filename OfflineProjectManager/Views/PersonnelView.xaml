<UserControl x:Class="OfflineProjectManager.Views.PersonnelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>

        <!-- List -->
        <DataGrid Grid.Column="0" ItemsSource="{Binding PersonnelHelper}" SelectedItem="{Binding SelectedPersonnel}"
                  AutoGenerateColumns="False" CanUserAddRows="False" Background="#252526" RowBackground="#252526" Foreground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Region" Binding="{Binding Region}" Width="100"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Editor -->
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Edit / Add Personnel" Foreground="Cyan" FontSize="16" Margin="0,0,0,10"/>
            
            <TextBlock Text="Name" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedPersonnel.Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Phone" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedPersonnel.Phone, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Region" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedPersonnel.Region, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Role" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedPersonnel.Role, UpdateSourceTrigger=PropertyChanged}"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save Selected" Command="{Binding SavePersonnelCommand}" Padding="10,5" Margin="0,0,5,0" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                    <Button Content="Delete" Command="{Binding DeletePersonnelCommand}" Padding="10,5" Background="#C42B1C" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <Separator Margin="0,20" Background="Gray"/>
            
            <TextBlock Text="New Person" Foreground="Cyan" FontSize="14" Margin="0,0,0,10"/>
            <TextBox Text="{Binding NewPersonName}" ToolTip="Name"/>
            <TextBox Text="{Binding NewPersonPhone}" ToolTip="Phone"/>
            <TextBox Text="{Binding NewPersonRegion}" ToolTip="Region"/>
            <TextBox Text="{Binding NewPersonRole}" ToolTip="Role"/>
            <Button Content="Add New" Command="{Binding AddPersonnelCommand}" HorizontalAlignment="Stretch" Padding="10" Background="#28A745" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
