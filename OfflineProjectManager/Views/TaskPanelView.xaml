<UserControl x:Class="OfflineProjectManager.Views.TaskPanelView"
             x:Name="TaskPanelRoot"
             xmlns:vm="clr-namespace:OfflineProjectManager.Features.Task.ViewModels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:OfflineProjectManager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="{DynamicResource PanelBrush}">
    
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        
        <!-- Shared context menu for Notes -->
        <ContextMenu x:Key="NotesContextMenu" DataContext="{Binding DataContext, Source={x:Reference TaskPanelRoot}}">
            <MenuItem Header="✏️ Edit Note" Command="{Binding EditTaskCommand}"/>
            <MenuItem Header="✅ Mark Completed" Command="{Binding ToggleCompletedCommand}"/>
            <MenuItem Header="👁️ View Original Content" Command="{Binding ViewOriginalContentCommand}"/>
            <Separator/>
            <MenuItem Header="🗑️ Delete" Command="{Binding DeleteItemCommand}"/>
        </ContextMenu>
        
        <!-- Shared context menu for Tasks -->
        <ContextMenu x:Key="TasksContextMenu" DataContext="{Binding DataContext, Source={x:Reference TaskPanelRoot}}">
            <MenuItem Header="✏️ Edit Task" Command="{Binding EditTaskCommand}"/>
            <MenuItem Header="✅ Mark Completed" Command="{Binding ToggleCompletedCommand}"/>
            <MenuItem Header="👁️ View Original Content" Command="{Binding ViewOriginalContentCommand}"/>
            <Separator/>
            <MenuItem Header="🗑️ Delete" Command="{Binding DeleteItemCommand}"/>
        </ContextMenu>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="*"/>    <!-- Tabs (List) -->
            <RowDefinition Height="Auto"/> <!-- Input Form -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Task Manager" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="{DynamicResource ForegroundBrush}"/>

        <!-- Tabs (Flat Text Style like Python) -->
        <TabControl Grid.Row="1" Margin="0,0,0,10" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Margin" Value="0,0,5,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" Background="Transparent" Padding="{TemplateBinding Padding}" BorderThickness="0,0,0,2" BorderBrush="Transparent">
                                    <ContentPresenter x:Name="ContentSite"
                                                    VerticalAlignment="Center"
                                                    HorizontalAlignment="Center"
                                                    ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                                        <Setter Property="Opacity" Value="0.8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </TabControl.ItemsPanel>

            <TabItem Header="Notes">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Notes List -->
                    <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0">
                         <ListBox x:Name="NotesListBox" Background="Transparent" BorderThickness="0" 
                                  ItemsSource="{Binding Notes}" SelectedItem="{Binding SelectedNote}" 
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch"
                                  MouseDoubleClick="NotesListBox_MouseDoubleClick">
                             <ListBox.ContextMenu>
                                 <StaticResource ResourceKey="NotesContextMenu"/>
                             </ListBox.ContextMenu>
                             <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,8" Margin="0">
                                        <TextBlock Text="{Binding Title}" FontWeight="Normal" Foreground="{DynamicResource ForegroundBrush}" TextTrimming="CharacterEllipsis"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem Header="Tasks">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Tasks List -->
                    <Border Grid.Row="0" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="0">
                         <ListBox x:Name="TasksListBox" Background="Transparent" BorderThickness="0" 
                                  ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" 
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch"
                                  MouseDoubleClick="TasksListBox_MouseDoubleClick">
                            <ListBox.ContextMenu>
                                <StaticResource ResourceKey="TasksContextMenu"/>
                            </ListBox.ContextMenu>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="5,8" Margin="0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="Normal" 
                                                       Foreground="{DynamicResource ForegroundBrush}" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Status}" FontSize="10" Opacity="0.6"
                                                       Foreground="{DynamicResource ForegroundBrush}" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Input Form -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Name -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NewItemName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                     Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

            <!-- Desc -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Desc:" VerticalAlignment="Top" Margin="0,5,10,10" Foreground="{DynamicResource ForegroundBrush}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NewItemDescription, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"
                     AcceptsReturn="True"
                     Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource ForegroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"/>

            <!-- Start Date -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Start:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{DynamicResource ForegroundBrush}"/>
            <DatePicker Grid.Row="2" Grid.Column="1" SelectedDate="{Binding NewItemStartDate}" Margin="0,0,0,10" 
                        IsEnabled="{Binding IsEditing}"
                        Foreground="{DynamicResource ForegroundBrush}" Background="{DynamicResource InputBackgroundBrush}"/>

            <!-- End Date -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="End:" VerticalAlignment="Center" Margin="0,0,10,15" Foreground="{DynamicResource ForegroundBrush}"/>
            <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding NewItemEndDate}" Margin="0,0,0,15"
                        IsEnabled="{Binding IsEditing}"
                        Foreground="{DynamicResource ForegroundBrush}" Background="{DynamicResource InputBackgroundBrush}"/>

            <!-- Buttons -->
            <Grid Grid.Row="4" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Show Save button when editing -->
                <Button Grid.Column="0" Grid.ColumnSpan="3" Content="💾 Save Changes" 
                        Command="{Binding SaveCommand}" Height="35"
                        Visibility="{Binding IsEditing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                
                <!-- Show Add buttons when not editing -->
                <Button Grid.Column="0" Content="Add Note" Command="{Binding AddNoteCommand}" Height="35"
                        Visibility="{Binding IsReadOnly, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <Button Grid.Column="2" Content="Add Task" Command="{Binding AddTaskCommand}" Height="35"
                        Visibility="{Binding IsReadOnly, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
