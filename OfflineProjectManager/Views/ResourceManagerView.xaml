<UserControl x:Class="OfflineProjectManager.Views.ResourceManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OfflineProjectManager.Views"
             xmlns:vm="clr-namespace:OfflineProjectManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#1E1E1E">
        <TabControl Background="Transparent" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <!-- Personnel Tab -->
            <TabItem Header="Personnel" Background="#2D2D30" Foreground="White">
                <local:PersonnelView />
            </TabItem>

            <!-- Contracts Tab -->
            <TabItem Header="Contracts / Contractors" Background="#2D2D30" Foreground="White">
                <local:ContractView />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
