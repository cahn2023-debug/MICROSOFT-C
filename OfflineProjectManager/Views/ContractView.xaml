<UserControl x:Class="OfflineProjectManager.Views.ContractView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <!-- List -->
        <DataGrid Grid.Column="0" ItemsSource="{Binding ContractHelper}" SelectedItem="{Binding SelectedContract}"
                  AutoGenerateColumns="False" CanUserAddRows="False" Background="#252526" RowBackground="#252526" Foreground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Contractor" Binding="{Binding ContractorName}" Width="150"/>
                <DataGridTextColumn Header="Package" Binding="{Binding BiddingPackage}" Width="100"/>
                <DataGridTextColumn Header="Code" Binding="{Binding ContractCode}" Width="80"/>
                <DataGridTextColumn Header="Region" Binding="{Binding Region}" Width="80"/>
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Editor -->
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Edit / Add Contract" Foreground="Cyan" FontSize="16" Margin="0,0,0,10"/>
            
            <TextBlock Text="Contractor" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedContract.ContractorName, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Package" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedContract.BiddingPackage, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Code" Foreground="Gray"/>
            <TextBox Text="{Binding SelectedContract.ContractCode, UpdateSourceTrigger=PropertyChanged}"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="Volume" Foreground="Gray"/>
                    <TextBox Text="{Binding SelectedContract.Volume, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Unit" Foreground="Gray"/>
                    <TextBox Text="{Binding SelectedContract.VolumeUnit, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Save Selected" Command="{Binding SaveContractCommand}" Padding="10,5" Margin="0,0,5,0" Background="#007ACC" Foreground="White" BorderThickness="0"/>
                    <Button Content="Delete" Command="{Binding DeleteContractCommand}" Padding="10,5" Background="#C42B1C" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <Separator Margin="0,20" Background="Gray"/>
            
            <TextBlock Text="New Contract" Foreground="Cyan" FontSize="14" Margin="0,0,0,10"/>
            <TextBox Text="{Binding NewContractorName}" ToolTip="Name"/>
            <TextBox Text="{Binding NewContractPackage}" ToolTip="Package"/>
            <Button Content="Add New" Command="{Binding AddContractCommand}" HorizontalAlignment="Stretch" Padding="10" Background="#28A745" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</UserControl>
